// This config file is used on tests
// which means we can use a different db for testing like h2
env = "test"
service = "todo"

db = {
  url = "jdbc:h2:mem:test"
  driver = org.h2.Driver
  connectionPool = disabled
  keepAliveConnection = true
}
